# OCR/ICR Data Extraction Pattern

Location: `patterns/OCR/`

Pattern files:
- `index.html` — Page entry point
- `entry.jsx` — React entry, renders PatternDocument with ocrPatternData
- `patternData.js` — Pattern content and metadata
- `README.md` — This file

Images: Add screenshots to `public/img/OCR/` and reference in patternData.js as `/img/OCR/filename.png`.

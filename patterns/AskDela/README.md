# Ask Dela Pattern

Location: `patterns/AskDela/`

Pattern files:
- `index.html` — Page entry point
- `entry.jsx` — React entry, renders PatternDocument with pattern data
- `patternData.js` — Pattern content and metadata
- `README.md` — This file

Images: Add screenshots to `public/img/AskDela/` and reference in patternData.js as `/img/AskDela/filename.png`.
